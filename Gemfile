# Ruby dependencies for the release toolchain. CI installs these with
# `bundle install`; run `bundle exec fastlane <lane>` so the pinned fastlane is used.
source "https://rubygems.org"

gem "fastlane", "~> 2.221"
